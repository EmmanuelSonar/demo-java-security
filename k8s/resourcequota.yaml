apiVersion: v1
kind: ResourceQuota
metadata:
  name: java-security-quota
  namespace: java-security
spec:
  hard:
    requests.cpu: "2"
    requests.memory: 1Gi
    limits.cpu: "4"
    limits.memory: 2Gi
    pods: "10"
    services: "5"
    persistentvolumeclaims: "5"
